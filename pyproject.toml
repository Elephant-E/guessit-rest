[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "guessit-rest"
version = "0.1.0"
dependencies = [
    "flask",
    "guessit",
    "flask-cors",
    "flask-restful>=0.3.6"
]
